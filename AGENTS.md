# Repository workflow

- After every substantial code or configuration change, restart the local development server and confirm the application is reachable before handing work back to the user.
- Use `npm run dev` for the local server unless the task requires a different command.
- Report the active local URL in the final handoff.
